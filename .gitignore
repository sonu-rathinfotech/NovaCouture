# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets. `*.local` above does NOT cover a plain `.env`, which is how
# service-role keys and database passwords end up in git history.
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Generated by the db tooling. review-accounts.txt and admin-account.txt hold
# real credentials in plain text, and the apply-*.sql dumps are throwaway.
supabase/.generated/
